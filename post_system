#게시글 작성 관련 코드
import streamlit as st

with st.form(key="post_form"):
    title = st.text_input("제목을 입력하세요")
    content = st.text_area("내용을 입력하세요", height=200)
    submit_button = st.form_submit_button(label="게시글 등록")

if submit_button:
    if title.strip() and content.strip():
        new_post = {
            "title": title,
            "content": content
        }

    st.session_state.posts.append(new_post)
    st.success("게시글이 등록되었습니다.")
    st.rerun() #화면 갱신
else:
    st.warning("제목과 내용을 모두 입력해주세요.")

#참고:
#import streamlit as st

# components 폴더 안의 post_form 파일에서 render_post_form 함수를 가져옴.
#from components.post_form import render_post_form

#st.title("게시판")

#if st.button("✏️ 글쓰기"):
    #render_post_form()

#이 형식으로 쓰면 됨 from [폴더명].[파일명] import [함수명] 형태
#📁내-프로젝트-폴더/
# ├── 📄app.py
# └── 📁components/        <-- 하위 폴더
#      └── 📄post_form.py   <-- 폴더 안의 파일
# -> from components.post_form import render_post_form
